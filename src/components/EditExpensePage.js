import React from "react";

const EditExpensePage = () => {
    return (
        <div>
            this from my edit Expense component number {props.match.params.id}
        </div>
    );
};
export default EditExpensePage;
